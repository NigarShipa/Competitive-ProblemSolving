#include<bits/stdc++.h>
using namespace std;
int main()
{

  int x;
  cin>>x;
  string a;
  for(int i=0;i<x;i++)
  {
    cin>>a;
    if(a.length()>10)
    {
       int count=a.length()-2;
       cout<<a[0]<<count<<a[count+1]<<endl;
    }
    else
    {
  cout<<a<<endl;
    }
  }
}
